import { ListItemDetailDirective } from "./list-item-detail.directive";

describe("MatchingDetailDirective", () => {
  it("should create an instance", () => {
    const directive = new ListItemDetailDirective();
    expect(directive).toBeTruthy();
  });
});
