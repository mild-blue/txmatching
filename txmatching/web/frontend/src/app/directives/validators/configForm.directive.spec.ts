import { PatientDirective } from './configForm.directive';

describe('PatientDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientDirective();
    expect(directive).toBeTruthy();
  });
});
